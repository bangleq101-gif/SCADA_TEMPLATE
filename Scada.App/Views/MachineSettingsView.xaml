<UserControl x:Class="Scada.App.Views.MachineSettingsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:Scada.App.Controls">
 <controls:WorkspaceLayout Title="Machine Settings" Description="Machine-specific settings workspace." Style="{StaticResource WorkspaceLayoutStyle}"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="240"/><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
  <StackPanel><Border Style="{StaticResource CardBorderStyle}"><TreeView ItemsSource="{Binding PageGroups}" ItemTemplate="{StaticResource MachineSettingsPageGroupTemplate}" SelectedItemChanged="OnSelectedPageChanged" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True"/></Border><CheckBox Content="Show hidden configuration" IsChecked="{Binding ShowHiddenConfiguration}" Margin="0,8,0,0"/></StackPanel>
  <Grid Grid.Column="2"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><StackPanel><TextBlock Text="{Binding SelectedPage.Title}" Style="{StaticResource CardTitleTextStyle}"/><TextBlock Text="{Binding SelectedPage.Description}" Style="{StaticResource WorkspaceDescriptionTextStyle}"/><TextBlock Text="{Binding LastErrorMessage}" Foreground="OrangeRed"/></StackPanel><ItemsControl Grid.Row="1" ItemsSource="{Binding ValidationIssues}"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding Message}" TextWrapping="Wrap"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Foreground" Value="{StaticResource WarningBrush}"/><Style.Triggers><DataTrigger Binding="{Binding IsBlocking}" Value="True"><Setter Property="Foreground" Value="{StaticResource BadBrush}"/><Setter Property="FontWeight" Value="SemiBold"/></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock></DataTemplate></ItemsControl.ItemTemplate></ItemsControl><ListBox Grid.Row="2" ItemsSource="{Binding SelectedPage.PresentationRows}" BorderThickness="0" Background="Transparent" ScrollViewer.CanContentScroll="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"/><WrapPanel Grid.Row="3" Margin="0,12,0,0"><Button Content="Apply page" Command="{Binding ApplyPageCommand}" Style="{StaticResource PrimaryButtonStyle}"/><Button Content="Revert page" Command="{Binding RevertPageCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0"/><Button Content="Save project" Command="{Binding SaveProjectCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="8,0,0,0"/><Button Content="Revert saved" Command="{Binding RevertProjectCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0"/><TextBlock Text="{Binding SelectedPage.ApplyStatus}" Margin="12,4,0,0"/></WrapPanel></Grid>
 </Grid></controls:WorkspaceLayout>
</UserControl>
